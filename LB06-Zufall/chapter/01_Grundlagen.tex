% LTeX: language=de

\subsection{Grundlagen}

\begin{note}{}
Ein \texthl{\textbf{Zufallsexperiment (ZE)}} ist ein Vorgang / Epxeriment dessen Resultate / Ergebnisse (mindestens 2 verschiedene) \uline{zufällig eintreten} und das \uline{beliebig oft wiederholbar} ist.\\[-1.0ex]

Alle möglichen \texthl{\textbf{Ergebnisse}} eines ZEs werden in der \texthl{\textbf{Ergebnismenge (Ergebnisraum) $\Omega$}} (\textcolor{myg}{``Omega''}) zusammengefasst: 
\begin{equation*} 
    \Omega = \{ \omega_1; \omega_2; \ldots; \omega_n \} \qquad \qquad \omega_{i}: \quad \text{\textbf{Ergebnisse} des ZEs}
\end{equation*}
Jedes Resultat des ZEs ist dabei genau einem Ergebnis $\omega_i$ zugeordnet.

\begin{Example}{Würfelwurf}{}
    \vspace{-1cm}
    \begin{align*}
        \tikzmarknode{aa}{} 
        \overbrace{\Omega_{1}}^{\mathclap{\textcolor{myg}{\text{\footnotesize feinster Ergebnisraum}}}}
        &= \{1; 2; 3; 4; 5; 6\} \tikzmarknode{ab}{} 
        & 
        \overbrace{\abs{\Omega_{1}}}^{\mathclap{\text{\footnotesize\shortstack[l]{\texthl{\textbf{Mächtigkeit}} \textcolor{myg}{von $\Omega_{1}$}\\ \# Elemente}}}}
        &= 6\\[1.0ex]
        \tikzmarknode{ba}{} \Omega_{2} 
        &= \{6; \overbrace{\bar{6}}^{\textcolor{myg}{\text{\footnotesize keine} \ 6}}\} \qquad \tikzmarknode{bb}{}
        & \abs{\Omega_{2}} 
        &= 2 \\[1.0ex]
        \Omega_{3} 
        &= \{\text{gerade}; \text{ungerade}\} 
        & \abs{\Omega_{3}} 
        &= 2 \\[1.0ex]
        \Omega_{4} 
        &= \{\text{gerade}; 1; 3; 5\} 
        & \abs{\Omega_{4}} 
        &= 4 \\[1.0ex]
        \Omega_{5} 
        &= \{\text{gerade}; \text{Primzahl}\} 
        \qquad \qquad \textcolor{myr}{\mathrlap{\text{\parbox[t]{5cm}{\footnotesize kein Ergebnisraum, da $2$\\ beiden zugeordnet werden kann}}}}
        & &
    \end{align*}
    \begin{tikzpicture}[remember picture, overlay,font=\footnotesize, color=myg]
        \draw[-latex] ([anchor=north west, xshift=0mm, yshift=0mm]aa) coordinate (r)
            to [out=210,in=150,looseness=1] 
            node[midway, anchor=east, xshift=0mm, yshift=0mm] {Vergröberung}
            ([anchor=north west, xshift=0mm, yshift=0mm]ba -| r);
        \draw[-latex] ([anchor=north east, xshift=0mm, yshift=0mm]bb) coordinate (r)
            to [out=30,in=330,looseness=1] 
            node[midway, anchor=west, xshift=0mm, yshift=0mm] {Verfeinerung}
            ([anchor=north east, xshift=0mm, yshift=0mm]ab -| r);
    \end{tikzpicture}
    \vspace{-2.0ex}
\end{Example}
\end{note}

\begin{Exercise}{S. 147/1}{}
\begin{enumerate}[leftmargin=*]
    \item [1.]{
        \begin{enumerate}[label=\alph*),leftmargin=*]
        \item[a)] {
            \texthl{\textbf{Baumdiagramme}} veranschaulichen mehrstufige ZEs (d. h. mehrere Aktionen)
            \begin{minipage}[t]{0.35\linewidth}
            \raisebox{-\height}{
            \begin{tikzpicture}[
                grow=right,
                level distance=25mm,
                level 1/.style={sibling distance=24mm},
                level 2/.style={sibling distance=7mm},
                edge quotes/.style={auto, font=\footnotesize},
                every node/.style={circle}
            ]
            \node (root) {}
            child { node (B) {$b$} {}
                child { node (BB) {$b$} {}}
                child { node (BG) {$g$} {}}
                child { node (BR) {$r$} {}}
            }
            child { node (G) {$g$} {}
                child { node (GR) {$r$} {}}
                child { node (GG) {$g$} {}}
                child { node (GB) {$b$} {}}
            }
            child { node (R) {$r$} {}
                child { node (RR) {$r$} {}}
                child { node (RG) {$g$} {}}
                child { node (RB) {$b$} {}}
            };

            % ---- level descriptions ----
            \begin{scope}[every node/.style={inner sep=0pt, outer sep=0pt}]
                % Center of first level (between B and R)
                \node[font=\footnotesize\bfseries, anchor=base] at ($ (R) + (0,12mm) $) {1. Ziehung};
                % Center of second level (between BB and RB)
                \node[font=\footnotesize\bfseries, anchor=base] at ($ (RB) + (0,5mm) $) {2. Ziehung};
            \end{scope}

            \end{tikzpicture}
            }
            \end{minipage}
            \hfill
            \begin{minipage}[t]{0.55\linewidth}
                \vspace{-2.0ex}
                \begin{align*}
                    \Omega &= {\begin{array}[t]{l}
                                \{ (r; b); \ (r; g); \ (r; r)\\
                                (g; b); \ (g; g); \ (g; r)\\
                                (b; g); \ (b; r) \}
                                \end{array}}
                                \\
                    \abs{\Omega} &= 9
                \end{align*}
            \end{minipage}
            }
        \item[b)] {
            Ein \texthl{\textbf{Ereignis}} ist eine Teilmenge von $\Omega$, d. h. jedes Element in $E$ muss in $\Omega$ liegen.\\[1.0ex]
            z. B.: \qquad {
                \begin{minipage}[t]{0.7\linewidth}
                    \vspace{-5.0ex}
                    \begin{align*}
                        A &= \{ (r; r); \ (b; r); \ (g; r) \} \quad \textcolor{myg}{\text{\footnotesize (aufzählende Mengenschreibweise)}}\\
                        \abs{A} &= 3 \\
                        A: & \ \text{``Die zweite gezogene Kugel ist rot''}
                \end{align*}
                \end{minipage}
            }\\[1.0ex]
            \begin{itemize}[leftmargin=*]
                \item {
                    \texthl{\textbf{Sicheres Ereignis}}: tritt immer ein
                    \begin{align*}
                        &E_{1}: \ \text{``Beim ersten Ziehen wird} \ r \text{,} \ b \ \text{oder} \ g \ \text{gezogen''}\\
                        &E_{1} = \Omega
                    \end{align*}
                    }
                \item {
                    \texthl{\textbf{Unmögliches Ereignis}}: tritt nie ein
                    \begin{align*}
                        &E_{2}: \ \text{``Die zweite gezogene Kugel ist schwarz''}\\
                        &E_{2} = \{ \ \} = \emptyset \quad \textcolor{myg}{\text{\}\quad \footnotesize Leere Menge}}
                    \end{align*}
                    }
                \item {
                    \texthl{\textbf{Elementarereignis}}: enthält genau ein Element aus $\Omega$
                    \begin{align*}
                        &E_{3}: \ \text{``Zwei blaue Kugeln werden gezogen''}\\
                        &E_{3} = \{ (b; b) \}
                    \end{align*}
                    }
            \end{itemize}
            \begin{align*}
                E_{1} &= \{ (r; r); \ (b; b); \ (g; g) \} & \abs{E_{1}} = 3 &&\\
                E_{2} &= \{ (b; b); \ (r; b); \ (g; b); \ (b; r); \ (b; g) \} & \abs{E_{2}} = 5 &&\\
                E_{3} &= \{ (r; g); \ (g; g); \ (b; g) \} & \abs{E_{3}} = 3 &&
            \end{align*}
            }
    \end{enumerate}
    }
\end{enumerate}
\end{Exercise}
